const model = require("./post.model");
const resolver = require("./post.resolver");
const service = require("./post.service");
const schema = require("./post.schema");

module.exports = {
  model,
  resolver,
  service,
  schema,
};
